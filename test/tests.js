"use strict";

/**
 * Tests of reports code
 */


var assert = require("assert");
const reports = require("../routes/reports.js");

describe("example of a test", function() {
    it("It should have an assert", function() {
        assert.equal(true, true);
    });
});
